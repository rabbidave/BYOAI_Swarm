[project]
name = "repl-nix-attachmentanalyzer"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "docker>=7.1.0",
    "flask>=3.0.3",
    "pyyaml>=6.0.2",
    "swarm>=0.0.2",
]
